#include "registry/registry.h"



namespace ddknd::registry
{
    // Entity



    // Registry::id_type Registry::GetEntity(std::uint32_t index)
    // {
    //     std::uint32_t idx = index;
    //     if (idx >= gens_.size())
    //     {
    //         return id_type::Invalid();
    //     }
    //     std::uint32_t gen = gens_[idx];

    //     std::cerr << "Get Entity: gen=" << std::bitset<32>(gen) << " idx=" << std::bitset<32>(idx) << "\n";
    //     return id_type{gen, idx};
    // }


} // namespace ddknd::registry