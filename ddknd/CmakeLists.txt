add_library(ddknd STATIC)

target_compile_features(ddknd PUBLIC cxx_std_20)

file(GLOB_RECURSE ddknd_SRC CONFIGURE_DEPENDS
    ${CMAKE_CURRENT_SOURCE_DIR}/src/*.cpp
    ${CMAKE_CURRENT_SOURCE_DIR}/src/*.c
)

target_sources(ddknd 
    PRIVATE 
        ${ddknd_SRC}
)

target_include_directories(ddknd 
    PUBLIC
        ${CMAKE_CURRENT_SOURCE_DIR}/include
    PRIVATE
        ${CMAKE_CURRENT_SOURCE_DIR}/src
)
 
# link
target_link_libraries(ddknd 
    PRIVATE
        glfw
        glm::glm
        fmt::fmt
        glad::glad
        cgltf::cgltf
        stb::stb
    PUBLIC
        spdlog::spdlog
)

# commands Qvec-report:2
if(MSVC)
#    target_compile_options(ddknd PRIVATE 
#            $<$<CONFIG:Release>:/Qvec-report:2>)

    # to display debug symbol
    target_compile_options(ddknd PRIVATE
        "$<$<CONFIG:Release>:/Zi>"
    )

    target_link_options(ddknd PRIVATE
        "$<$<CONFIG:Release>:/DEBUG>"
    )
endif()